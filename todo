TODO

automatic update issues (yes/no prompt)
split up the primary aliases
write a different repo to check for new updates of packages bot
  lens needs to be in there (check version and alert when found)
  add for the submodules as well
consider moving the submodules outside of the repo
posh-git-sh isn't working right now
make a general update app for cron
allow azure data studio like code with extensions
fix the windows install
support other "basic-setup" style repos (for private secrets)
support yum, pacman, and apk
create a versioning system
